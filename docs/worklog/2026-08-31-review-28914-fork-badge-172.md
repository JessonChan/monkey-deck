# 2026-08-31 复审 #28914 fork 徽章前端面(#172)——APPROVE

## 起因

复审 task #28914(coder 提交 bb576db + 828f131):#172 session 行 fork 徽章可见性卡。规格:带 `forked_from` 的 session 行在 meta 簇 harness icon 同区渲染 GitFork 小徽章,悬停 tooltip「分叉自 …」(源标题优先、短 id 兜底),原生 session 不显示;与 Pencil(#154 两槽位)和 pin/闹钟槽位共存,行高不变。

## 验证点(D1-D8 反向实证,均通过)

- **D1 改动面与红线**:`git show --stat bb576db` 仅 5 个前端文件(Sidebar.tsx +29 / index.css +7 / zh+en.json 各 +1 / 新 mount 测试 +206);工作树干净,fork 业务链路(ForkSession/门控)零触及;#154 renamedMark 两槽位(971/975)、pin/terminal/scheduled 守卫(997/1002/1007)一行未动。
- **D2 类型补丁反向追踪(全链路消费)**:Go `Session.ForkedFrom`(`internal/store/store.go:101`,`json:"forkedFrom,omitempty"`)→ 本机重生 bindings(`wails3 generate bindings`)实证生成模型含 `forkedFrom`(models.js:625)→ `s.forkedFrom` 在 Sidebar.tsx:987 作渲染门控 → tooltip 值经 `forkSrcTitleById.get()` 查表。字段无死点;「类型补齐如有缺」 vacuous——类型属 codegen 产物,重生即含,无需手写。bindings 目录 gitignore 不入库,与该 commit 无关属正常。
- **D3 断言锚定值(非字段存在)**:新测试断言 tooltip 内容含解析出的源标题「源会话定制名」(源会话放另一项目,钉死跨项目全量解析 + customTitle 优先)、回退前缀 `deadbeef`、zh/en 文案逐字钉死(引真实 locale JSON)、徽章 DOM 紧邻 harness icon(`previousElementSibling` 类断言)。经 i18n mock 实现核实,断言走值流非键存在。
- **D4 规格逐条**:①显示条件 = `s.forkedFrom` 真值(undefined 与空串均不渲染,两用例覆盖);②tooltip 源标题优先、`slice(0,8)` 短 id 兜底(JS slice 天然安全,无 §5.4#3 风险;查表值为空串时经 `||` 同样落到 id 兜底);③位置 = `session-item-main` 内 HarnessIcon 之后、pinned 之前,meta 簇同区;④行高 = `.session-fork-mark` 逐字镜像 `.session-renamed` 持久标记族(`flex-shrink:0; display:inline-flex;` + text-3 安静档),GitFork size 12 与既有 HarnessIcon/terminal(均 12)同档,不抬高行盒;⑤i18n zh/en 同键同位(两文件 81 行),`{{src}}` 插值与 harnessTip/scheduledTip 同款。
- **D5 独立复跑**:新测试 5/5;Sidebar 全家 46/46(8 文件,含 renamed/pin/scheduled/fork/stray-zero 等同渲染块回归);全量 `bun test --isolate` **524 pass / 0 fail**(72 文件,74s)与 worklog 声称一致;`bunx tsc --noEmit` exit 0(strict,基于重生 bindings)。worklog 声明与实测零偏差。
- **D6 状态与派生正确性**:`forkSrcTitleById` useMemo 依赖 `[props.sessionsByProject]`,与相邻 `allSessionIds` 同款同源快照;`!m.has` 首见优先在全局唯一 session id(worktree 模型)下无害。
- **D7 纪律**:新注释全英文(§3.7);worklog 完整(起因/改法/文件/验证/下一步)且含 index.css 改动面偏差说明(行高验收所必需,与家族 5 标记类同构,非第二套约定)与 §4.7 三端论证(纯数据条件渲染、无 remote 守卫/断点 CSS/事件通道,三端同一渲染路径);feat 与 docs 原子分提交,无夹带无产物。
- **D8 测试边界**:mount 测试 mock bindings/react-tooltip/react-i18next,零真后端调用(§5.1);dynamic import 在 mock 注册之后,骨架沿既有 Sidebar.*.mount 测试。

## 非阻断观察(不要求改)

1. 徽章 span 与 pin/renamed/popout 全家族一样无 aria-label——hover 语义由 react-tooltip 承担(§4.5),家族既有模式,非本卡回归;将来若做读屏适配应整族统一处理。
2. 源会话被删除后徽章仍显示(id 兜底)——符合「never a broken badge」降级设计,产品语义合理。
3. 桌面真机目验(徽章间距/tooltip 手感)coder 已列为 OPEN 留待 `wails3 dev` 顺带确认,纯可见性卡不阻塞。

## 结论

**APPROVE**。规格五项(显示条件/tooltip 文案/源标题优先短 id 兜底/槽位共存/行高不变)全落地,类型链路 codegen 闭环无死字段,断言全部锚定值,全量 524 绿 + tsc 0 独立复跑。按流程停 completed-ready:不 push、不关 issue。
