# Review #28396 P0 三件前端面复审(collectTags null / tsc 门 / coverage 基准)(#28397)

日期:2026-08-29
被审:5f19da0(collectTags 纳 null)+ 14f2576(Makefile tsc 门/WAILS3 恒温)+ 69cb3b1(coverage 基准)+ 7e21000(worklog)
结论:**APPROVE**——三件全实证通过,零阻塞 finding;3 条 P3 观察留档。

## 复审方法

按「类型补丁反模式」反向追踪:不从 commit message 出发,从字段定义点沿消费链逐点实证;门类改动做负向测试(还原 bug 验证门真的拦截);gate 类改动本机全量重跑。

## 逐件验证(全部本机实证,非采信 worklog)

### ① collectTags 纳 null——类型链全通

- **wire 实证**:`frontend/bindings` 本地缺失,用 go.mod 钉死版本重新生成(`go run …wails3@v3.0.0-alpha2.106 generate bindings -clean=true -ts -i`),26 models 与 worklog 声明一致;`store/models.ts:187` 逐字确认 `"tags": string[] | null`。
- **消费链反溯**:Sidebar Props `sessionsByProject: Record<string, Session[]>`(`Sidebar.tsx:27`,Session 来自生成 binding model)→ `collectTags(projSessions)`(`Sidebar.tsx:721`)→ 签名 `{ tags?: string[] | null }[]`(`tagColor.ts:33`)→ 运行时 `s.tags ?? []`(`tagColor.ts:37`)。定义→wire→消费→运行时四点全接通,非类型补丁。
- **测试锚定值**:`tagColor.test.ts` 断言 `toEqual(["api","db","ui"])`、`toEqual([])`、`toEqual(["api"])`——锚定输出值而非字段存在;4/4 pass(16 expects)。
- **负向闭环**:sed 还原旧签名 `{ tags?: string[] }[]` → 门报**恰好 1 错** `src/components/Sidebar.tsx(721,40): error TS2345`——与 worklog 声称的修前错误(位置/错误码/类型文案)逐字吻合;恢复新签名 → 0 错。复现→修复→复现消失闭环完成。

### ② tsc 无缓存门——有真实拦截力

- **位置与顺序**(`Makefile:60-70`):`cover: bindings`(bindings 自供给,tsc 能解析 model)→ `:61` node_modules 前置检查(缺依赖给人话报错)→ `:66` `bunx tsc --noEmit --incremental false` → `:67` go test。`cover-check: cover` 传递,两门都类型门控。
- **无缓存成立**:tsconfig.json 无 `incremental` 项 + 显式 `--incremental false` → 无 tsbuildinfo 可陈旧。
- **CI 不受影响**:`frontend/package.json:9` `"build": "tsc && vite build --mode production"`——CI 前端 job 本就含 tsc,声明属实。
- **拦截力实证**:上述负向测试即门的实测(旧签名被门抓住)。本 worktree 复审初跑门失败 230 错,根因是缺 `frontend/node_modules`(bunx 现场 fallback),`bun install` 后 0 错——顺带印证 `:61` 前置检查的必要性。
- **bindings flags 对齐核实**:Taskfile `generate:bindings`(`build/Taskfile.yml:191`)为 `-f '{{.BUILD_FLAGS}}' -clean=true -ts -i`;Taskfile **无全局 `vars:` 块**,`.BUILD_FLAGS` 恒为未定义→空串,`-f ''` 等价于省略——Makefile 的 `-clean=true -ts -i` 与 Taskfile 实际生效 flags 等价,无发散风险。全门跑通(tsc/bun test 均消费该 bindings)即产出可用性实证。

### ③ coverage 基准重定——棘轮纪律未被破坏

- **小数支持**:`scripts/coverage-floor.sh:29` 明示 "decimals from re-baselining are fine";`num_ok`/`ge` awk 数值比较,`floor_key` 按 `$1==k` 取值,`go 69.5`/`frontend 63.0` 解析无误(gate 实际输出 "63.0% >= floor 63.0%")。
- **方向正确**:go 69→69.5 是棘轮**上调**;frontend 64→63.0 回落实测,依据链完整(gate 自述救济路径「确认无测试损失后 --set」;worklog 取证:0 测试删除、stash 基线 63.02% 逐位吻合、稀释源为 #150–#158 新 UI)。复核与本次全门跑数一致。
- **本机全量重跑 `make cover-check`**:bindings 再生成 → tsc 门 → go test 覆盖率 → bun test **424 pass / 0 fail**(420+4 新,7430 expects)→ floor 全绿(go 69.5≥69.5;frontend 63.0≥63.0;分包 14/15 + internal/update 豁免,与 worklog 逐项吻合)。

### 附带 Makefile 修复

- **prerequisite bug**:`make -n build`/`make -n package` dry-run 均正常展开,不再 "No rule to make target 'wails3'"。
- **WAILS3 恒温化**:dry-run 展开为 `go run github.com/wailsapp/wails/v3/cmd/wails3@v3.0.0-alpha2.106`(与 go.mod 逐字一致);实测该版本生成 26 models,PATH 上 beta.3 无法再静默改写 wire 类型;`WAILS3=<path>` 覆盖语义保留(`?=`)。

## 三端矩阵

本次前端 diff 仅 lib 类型签名 + 新测试文件,零 UI/渲染面变化——worklog「三端矩阵不触发」判断正确(复核 diff 确认未触及任何组件/样式/manifest)。i18n 无新增 key(N/A);a11y 无 UI 面(N/A)。

## P3 观察(不阻塞,留档)

1. **test 文件在 tsc 门盲区**:`tsconfig.json:25` exclude `src/**/*.test.ts(x)`——新增 `tagColor.test.ts` 自身的类型错误 tsc 门不查(bun 剥类型也不查)。既有约定非本次引入,但意味着「类型回归只发生在 test 文件内」的场景门仍盲;若未来 test 内类型断言变重要,可评估去掉 exclude 或单开 test tsconfig。
2. **`Makefile:38` package 中文注释未随触及转英文**:`package:` recipe 行本次被改(去 prerequisite),其上方中文注释保留。§3.7「触及即转」边界情况,极轻微;下次触及 Makefile 该段时顺手转。
3. **OPEN #3(CI 前端 job 缺 bindings 供给)**:worklog 已正确留档为范围外。本地门已自供给,CI 侧 `bun run build` 的 tsc 对 `../../bindings/...` import 会 TS2307——建议后续单开小卡补 CI provisioning,避免 CI 前端 job 长期红或形同虚设。

## 结论

三件 P0 全部落地且经得起反溯:collectTags 四点消费链接通 + 锚定值契约测试;tsc 门位置/无缓存性/拦截力三实证;coverage 重定走 gate 自述救济且棘轮只升不降。commit 切分原子(每件一个 commit + worklog 独立),message 与实际改动一致。**APPROVE。**
