# 2026-08-31 复审 #28905 fork 探针 Phase1 后端面(#172)——APPROVE

## 起因

复审 task #28905(coder 提交 cb7cbf7 + 0cf4df6):#172 session/fork 的 Phase 1 纯探针卡。规格:①ConformanceReport 增 fork 声明位(未声明跳过往返);②undeclared 强发 `UnstableForkSessionRequest` 锚定 JSON-RPC 错误码;③declared 最小往返六问取证(+⑦fork 链 ⑧并发两个扩展);④fakeagent 双场景;⑤实测 8 harness 出矩阵,报告四段入 worklog。红线:零产品代码(capability.go/runner.go/chat.go/前端不动)、UNSTABLE 探针必收敛。

## 验证点(D1-D8 反向实证,均通过)

- **D1 红线(零产品代码)**:`git show --stat cb7cbf7` 仅 `internal/acp/probe.go`(+355)、`probe_fakeagent_test.go`(+272/-24)、`probe_test.go`(+35)、`probe_integration_test.go`(+5);工作树干净。`capability.go` 的 `SessionFork` 位为既有代码(8acd7bc 引入),本次零触及;探针自持 `Fork.Declared`,未给 capability.go 引入新消费者。
- **D2 类型补丁反向追踪**:ForkReport 全部 12 个字段逐一核销消费端——`Declared`→Summary 分支 + runForkProbes P2/P3 门控 + integration t.Logf;`Force`/`ForceClass`→Summary undeclared 行 + 单测断言;8 个 CheckResult→Summary forkMark 渲染 + declared 全行断言 + integration 日志;`Error`→Summary 异常行 + declared 测试断言。无死字段。
- **D3 断言锚定值(非存在性)**:undeclared 测试断言 `Force` 含 `-32601` 且 `ForceClass=method-not-found`(五分类逐值);declared 测试断言 `NewID.Note` 含 `fake-sess-2`(值流到具体输出)、八行全 `Pass=true`、`Force`/`Fork.Error` 为空;Summary 测试断言锚定串 `forced-fork: -32601 method not found: session/fork` 与 `n/a` 渲染。均为值级断言。
- **D4 逐路径收敛(UNSTABLE 红线)**:undeclared→强发+分类+八行 N/A+return;declared 前置 fork 失败→NewID 保留原因、其余七行 N/A 不被覆盖;declared 成功→八行逐项;panic→recover 落 `Fork.Error`。每 RPC 硬超时 20s(`probeForkTimeout`;prompt 走既有 90s),no-answer(超时/断连)单列分类。cancel 探针的 `cancelArmed` 在 fork 段前已 `Store(false)`(probe.go:397),fork 段 ②⑧ 的 prompt 不受 cancel wrapper 干扰。
- **D5 fakeagent 双场景真实性**:undeclared 场景 `sessionCapabilities:{}`→`sc.Fork==nil` 判 undeclared,`session/fork` 落 default 分支回 wire 级 `-32601`(走真实 JSON-RPC 往返,非 mock);declared 场景声明 fork/list/resume/close+loadSession,configOptions/modes 原样回显供 ⑤ 比对。prompt 位置计数(第 2 个=cancel probe)与探针 prompt 序列(main→cancel→②→⑧×2)确定性对齐。
- **D6 测试独立复跑**:`go test ./internal/acp/ -count=1` 全绿(6.2s,含两个新 fakeagent 场景 0.27s/3.11s + 两个 Summary 测试);`gofmt -l` 四文件干净;`go vet ./internal/acp/` 与 `go vet -tags integration ./internal/acp/` 均干净。worklog 声称「go build 因 frontend/dist 未构建 embed 报错」经查属实(`frontend/dist` 不存在,环境性,与本次无关;`go test` 已完整编译该包)。
- **D7 worklog 四段规格符合**:声明矩阵(8 harness+fakeagent 双场景)/错误码锚定(逐 harness 锚 `code message`,jcode -32603/-32601 不稳定如实记录)/六问逐项(omp ①-⑤⑦ 过、⑥ 换 cwd 确定性 cancelled、⑧ 并发杀死进程均如实)/结论(Phase 2 设计输入五条)。OPEN 项(junie/opencode/kimi 复测)显式列出,未夸大结论。
- **D8 SDK 契约核对**:acp-go-sdk v0.13.5 实测面——`ClientSideConnection.UnstableForkSession`(client_gen.go:275,method `session/fork`)、`UnstableForkSessionRequest.Cwd` 为必填(Validate)、响应含 `SessionId/ConfigOptions/Modes`、`SessionCapabilities.Fork *SessionForkCapabilities`(types_gen.go:4449)。探针构造与判据均正确。

## 非阻断观察(不要求改)

1. panic 兜底/前置失败路径下部分行可能零值渲染为 ✗ 而非 n/a(Fork.Error 行有解释;CanAdd=false 场景占主导,纯展示层细节)。
2. `echoVerdict` 对「双方均非 select 变体」的 configOption 不比对值(当前各家 configOptions 均为 select,fakeagent 亦然;探针级盲点)。
3. undeclared 但强 fork 成功(unexpected-success)时仍跳过往返——符合规格(P1 声明位门控),且已作为发现落报告。

## 结论

**APPROVE**。规格五项全落地,红线零违反,UNSTABLE 收敛有 fakeagent 双场景 + recover + 硬超时三重实证,测试断言全部锚定值。按流程停 completed-ready:不 push、不关 issue;实测矩阵回贴 GitHub 由人执行(P4 数据已在 worklog)。
