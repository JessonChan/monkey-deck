# 2026-09-01 review #28931:#177 复制 markdown 源(前端面)审查记录

## 起因

Task #28931:review Task #28930(coder 实现,commit 874375f + worklog ef94998)。规格:选区/整消息复制还原 markdown 源(position 溯源),改动面 = `lib/markdownSource.ts` + ChatView 接线(components map 挂 data-pos 属性)+ 测试;流程 coder→fe-reviewer→APPROVE,不 push 不关 issue。

## 审查结论:APPROVE(2 处缺口由 reviewer 当场补齐,见下)

### 逐链路核实(「类型补丁」反拍手检查,全部通电)

- `mdSourceProps(node)` → DOM `data-md-s/e`:`components` map 覆盖 p/li/td/ul/ol/blockquote/h1–h6/strong/em/del(`makeAnchored`)、pre→CodeBox 根、a、code(含 inline)、table→`.md-table-wrap` 根、th(含 coarsePointer 分支)。mount 测试逐选择器断言**字节级切片值**(p=`Intro with **bold** text.`、th=`| A `、`.md-table-wrap`=整表源、`.code-box`=完整围栏),并反向遍历管线元素确认无一漏挂。
- 消费端:`markdownSourceFromSelection` 读属性做 span 代数;Copy 动作 `rawSourceOfSelection()` 经 `data-md-msg={item.id}` 反查 item → `mdSourceOfItem`(渲染与查询共用同一拼接,流式 caret 一致)→ `copyTextQuiet((raw && md) || text)`。lib 16 例 + mount 5 例全部**锚定值断言**,无「字段存在」式断言。
- 防御面齐全:跨消息拒绝、无锚回退、collapsed/rangeCount=0/getSelection 缺失、流式 e 越界钳制、尾换行修剪、`e<s`/非整数拒绝。
- 附带修复属实:`node`(passNode)不再漏到 `<a>/<table>/<th>` DOM;ResizableTable/HeadCell 两条分支都解构 `node` 后再 spread。
- i18n:`selectionToolbar.copyMdTip` en/zh 同步(其余键 diff 干净);SelectionToolbar 以 `data-tooltip-content={t(a.tipKey)}` 真实消费(§4.5),`data-testid="selection-copy"` 保留(§4.2)。
- 工程量核对:`bun test --isolate` 548 pass / 0 fail、`tsc --noEmit` 干净(工作树需先 `wails3 generate bindings`——`frontend/bindings/` 是 gitignore 产物,非本次改动问题)。

### 缺口 1(P1,规格显式项):整消息 Copy 断言缺失

规格:「整消息 Copy 同改(若已拷原始串则**加断言维持**)」。前提成立——`MessageActions text={item.text}`(即原始源;流式中隐藏,不含 caret)——但全仓无任何测试断言 `copy-msg` 的复制载荷(fork 测试只断按钮存在),worklog 亦未提及该子句。**已补**:mount 用例点击 `[data-testid="copy-msg"]` 断言 `copied == [MD]`(整段原始源)。

### 缺口 2(P2,规格「math position 补测」+ 失实注释):math 行为零测试 + 注释失实

两处声称 math「已被 code/pre 路径覆盖 / anchored via code/pre」(components map 注释、worklog 决策节),但 PreRenderer 的 math 分支把 `src` 直接丢弃(`<MathBlock code={raw} />`)、CodeRenderer 的 math-inline 分支同样不挂锚——实际行为是**回退纯文本**(与 PreRenderer 自己的注释、worklog OPEN 一致)。规格的「math position 补测」完全没有对应测试。**已补**:

- mount 用例:选区内 math(块级 `$$..$$`,katex 按 `ChatView.math.mount.test.tsx` 同配方 stub)→ 断言 math 根无 `[data-md-s]` 祖先 + 剪贴板收到选区纯文本且不含 `$$`(回退语义锚定)。
- 勘误:ChatView components map 注释与 coder worklog 决策节改为如实描述(未挂锚 → 回退,挂锚留 OPEN)。

「给 MathBlock/MermaidRenderer 根透传锚」属任务面之外(需改 ChatView 之外文件),维持 coder 的 OPEN 不扩面。

## 改了哪些文件(review 增量)

- `frontend/src/components/ChatView.mdsource.mount.test.tsx`:+2 用例(整消息 Copy 原始源、math 回退);+ katex stub(仅本文件 mock)。
- `frontend/src/components/ChatView.tsx`:components map 注释勘误(纯注释,无行为变更)。
- `docs/worklog/2026-09-01-copy-markdown-source-177.md`:决策节 math 条目勘误。

## 验证

- 定向:`bun test src/components/ChatView.mdsource.mount.test.tsx src/lib/markdownSource.test.ts` → 23 pass(21 旧 + 2 新)。
- 全量:`bun test --isolate` → **550 pass / 0 fail**(548 + 2 新用例);`bunx tsc --noEmit` 干净。
- 三端(§4.7/§5.6):review 增量 = 测试 + 注释,零运行时行为变更(唯一行为面「整消息 Copy 已拷原始串」是既有实现的锚定,不是修改),三端结论继承 coder worklog(桌面 GUI / 远程浏览器 / PWA 预期零差异;三端人工冒烟仍记 OPEN,随上游条目跟进)。

## 下一步 / OPEN

- 继承 coder 条目:MathBlock/MermaidRenderer 根部挂锚(单独任务);三端人工冒烟各点一次选区复制。
- 按 RAK 流程 APPROVE 后置 completed-ready,不 push、不关 issue。
